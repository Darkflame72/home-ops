---
# No yaml-language-server schema available: kagent.dev/v1alpha2 RemoteMCPServer
apiVersion: kagent.dev/v1alpha2
kind: RemoteMCPServer
metadata:
  name: agentgateway-tools
  namespace: kagent
spec:
  description: Grafana (Prometheus/Loki) and ArgoCD tools via the shared agentgateway MCP proxy
  url: http://agentgateway-proxy.agents.svc.cluster.local/mcp
  protocol: STREAMABLE_HTTP
