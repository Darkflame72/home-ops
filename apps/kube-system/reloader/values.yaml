---
# No yaml-language-server schema available: stakater/Reloader (no values.schema.json)
fullnameOverride: reloader
reloader:
  readOnlyRootFileSystem: true
  podMonitor:
    enabled: true
    namespace: "{{ .Release.Namespace }}"
  deployment:
    resources:
      requests:
        cpu: 10m
        memory: 112Mi
      limits:
        memory: 448Mi
