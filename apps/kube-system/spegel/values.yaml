---
# No yaml-language-server schema available: spegel-org/spegel (no values.schema.json)
spegel:
  containerdSock: /run/containerd/containerd.sock
  containerdRegistryConfigPath: /etc/cri/conf.d/hosts
resources:
  requests:
    cpu: 20m
    memory: 128Mi
  limits:
    memory: 320Mi
service:
  registry:
    hostPort: 29999
serviceMonitor:
  enabled: true
