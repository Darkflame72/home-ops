---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./grafana-dashboard-generate.yaml
  - ./infra-exceptions.yaml
  # istio-ambient-namespace-labels.yaml and istio-default-authz.yaml are
  # deliberately unwired, not deleted — see the description in
  # istio-ambient-namespace-labels.yaml for why (broke CNPG/Dragonfly
  # cluster-wide, rolled back 2026-07-17 pending investigation in a
  # maintenance window).
  - ./volsync-generate.yaml
  - ./volsync-label-pvcs.yaml
  - ./disallow-latest-tag.yaml
  - ./disallow-privileged-containers.yaml
  - ./require-requests-limits.yaml
  - ./require-run-as-nonroot.yaml
