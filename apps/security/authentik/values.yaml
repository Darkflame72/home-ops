# No yaml-language-server schema available: goauthentik/helm (no values.schema.json)
global:
  podAnnotations:
    reloader.stakater.com/auto: "true"
  env:
    - name: AUTHENTIK_POSTGRESQL__HOST
      valueFrom:
        secretKeyRef:
          name: authentik-postgres-app
          key: host
    - name: AUTHENTIK_POSTGRESQL__NAME
      valueFrom:
        secretKeyRef:
          name: authentik-postgres-app
          key: dbname
    - name: AUTHENTIK_POSTGRESQL__USER
      valueFrom:
        secretKeyRef:
          name: authentik-postgres-app
          key: user
    - name: AUTHENTIK_POSTGRESQL__PASSWORD
      valueFrom:
        secretKeyRef:
          name: authentik-postgres-app
          key: password
    - name: AUTHENTIK_SECRET_KEY
      valueFrom:
        secretKeyRef:
          name: authentik-secret
          key: secret-key
authentik:
  log_level: info
  postgresql:
    port: 5432
    name: ""
    user: ""
    password: ""
  email:
    host: smtp-relay.default.svc.cluster.local
    use_tls: false
    port: 25
    from: "Authentik <noreply@darkflame.dev>"
postgresql:
  enabled: false
prometheus:
  rules:
    enabled: true
server:
  replicas: 1
  securityContext:
    runAsNonRoot: true
  resources:
    requests:
      cpu: 30m
      memory: 864Mi
    limits:
      memory: 3456Mi
  route:
    main:
      enabled: true
      hostnames: ["auth.darkflame.dev"]
      parentRefs:
        - group: gateway.networking.k8s.io
          kind: Gateway
          name: envoy-external
          namespace: network
          sectionName: https
  metrics:
    enabled: true
    serviceMonitor:
      enabled: true
worker:
  replicas: 2
  securityContext:
    runAsNonRoot: true
  resources:
    requests:
      cpu: 125m
      memory: 496Mi
    limits:
      memory: 1984Mi
