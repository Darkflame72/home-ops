---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: mc-router
  namespace: argocd
  finalizers:
  - resources-finalizer.argocd.argoproj.io
  annotations:
    argocd.argoproj.io/sync-wave: '0'
spec:
  project: default
  sources:
    - repoURL: "https://github.com/Darkflame72/home-ops.git"
      path: apps/network/mc-router
      targetRevision: main
      ref: repo
    - repoURL: ghcr.io/itzg/minecraft-server-charts
      chart: mc-router
      targetRevision: 1.4.0
      helm:
        releaseName: mc-router
        valueFiles:
          - $repo/apps/network/mc-router/values.yaml
  destination:
    name: in-cluster
    namespace: network
  syncPolicy:
    automated:
      allowEmpty: true
      prune: true
      selfHeal: true
    retry:
      limit: 1
      backoff:
        duration: 10s
        factor: 2
        maxDuration: 3m
    syncOptions:
      - CreateNamespace=true
      - ApplyOutOfSyncOnly=true
      - ServerSideApply=true
      - PruneLast=true
      - RespectIgnoreDifferences=true
